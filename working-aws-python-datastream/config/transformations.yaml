# ==================================================================================
# TRANSFORMATIONS CONFIGURATION
# ==================================================================================
# Maps transformation names to their corresponding Python transformer classes.
# Add new mappings here when you create new transformation classes.
# ==================================================================================

transformations:
  # =================================================================================
  # ✅ TRANSFORMATION 1: BID EVENTS RAW (Working Example)
  # =================================================================================
  bid_events_raw:
    class: "BidEventsRawTransformer"
    module: "transformations.bid_events_raw"
    description: "Transforms raw bid events with timestamp conversion"

  # =================================================================================
  # ✅ TRANSFORMATION 2: USER EVENTS ENRICHED (New Transformation)
  # =================================================================================
  user_events_enriched:
    class: "UserEventsEnrichedTransformer"
    module: "transformations.user_events_enriched"
    description: "Enriches user events with computed fields and timestamp conversion"

  # =================================================================================
  # 🔖 ADD YOUR NEW TRANSFORMATIONS HERE
  # =================================================================================
  # Example: User Events Processing
  # user_events_processed:
  #   class: "UserEventsProcessedTransformer"
  #   module: "transformations.user_events_processed"
  #   description: "Processes user activity events with enrichment"
  #
  # bid_events_aggregated:
  #   class: "BidEventsAggregatedTransformer"
  #   module: "transformations.bid_events_aggregated"
  #   description: "Aggregates bid events by time windows"
